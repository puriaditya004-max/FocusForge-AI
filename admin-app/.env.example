# Same backend, same API — this app does NOT talk to a different
# backend, it just serves a different, isolated frontend for it.
VITE_API_URL=http://localhost:5000/api

# In production (Vercel), set this to:
# VITE_API_URL=https://your-backend.onrender.com/api
